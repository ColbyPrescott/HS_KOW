#include "vex.h"

#include "Backend/utilityMath.h"

using namespace vex;

void AutonSkills() {
    // dualInertial.Calibrate();
    // wait(3, seconds);

    // aps.SetPosition(8, 72);
    // aps.SetRotation(DegreesToRadians(0));
    // storingRing = true;
    // mogoMover.open();

    // aps.SetDriving(true);

    // // Put preload onto alliance stake
    // // TriggerIntake();
    // MoveClosestHookToWaypoint(HWPs::waitForMogo, HWPs::depositRing);
    // wait(2, seconds);
    // intake.spinFor(-600, degrees);

    // // Drive to mogo
    // aps.AddPathPoint(24, 48, true, 0.3, 0.3, 5);
    // aps.EndPath();
    // // Pickup mogo
    // mogoMover.close();
    
    // // Drive mogo to the bottom left corner
    // aps.AddPathPoint(10, 10, true, 0.6, 0.6, 5);
    // aps.EndPath();
    // // Drop mogo
    // mogoMover.open();

    // // Drive a bit away
    // aps.AddPathPoint(40, 40, false, 0.3, 0.3, 5);
    // aps.EndPath();

    // // RAM!
    // aps.AddPathPoint(10, 10, false, 1, 1, 5);
    // aps.EndPath();

    // // Drive a bit away
    // aps.AddPathPoint(40, 40, false, 0.3, 0.3, 5);
    // aps.EndPath();
}